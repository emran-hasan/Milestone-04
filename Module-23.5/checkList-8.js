var arrayName = [element1, element2, element3];

// Change the third element of the array
arrayName[2] = newValue;

// Add two elements to the array
arrayName.push(element4, element5);

// Remove an element from the array
var removedElement = arrayName.splice(index, 1);
